# notify-new-inquiry (optional / unused)

Inquiry emails are sent from the Next.js contact form action (`src/lib/inquiry-email.ts`). This Edge Function is kept only as an unused backup and is **not required**.
